runConfig:
  minInstances: 0
  maxInstances: 3
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Configure SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY,
# and LEELA_APP_URL as runtime environment variables/secrets before deployment.
# See docs/SUPABASE_SETUP.md. Account data must never be stored in /tmp.
